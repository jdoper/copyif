<h1>Professores</h1>

<table>
  <thead>
    <tr>
      <th class="user_info">Nome</th>
      <th class="user_info">Matricula</th>
      <th class="user_info">Diretoria</th>
      <th class="user_info">Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @usuarios.each do |usuario| %>
      <tr>
        <td class="user_info"><%= usuario.nome %></td>
        <td class="user_info"><%= usuario.matricula %></td>
        <td class="user_info"><%= usuario.diretoria %></td>
        <td class="user_info"><%= usuario.tipo %></td>
        <td class="buttom_actions"><%= link_to 'Mostrar', usuario, id:$id %></td>
        <td class="buttom_actions"><%= link_to 'Editar', edit_usuario_path(usuario), id:$id %></td>
        <td class="buttom_actions"><%= link_to 'Excluir', usuario, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<table class="buttom_opt">
  <tr>
    <td class="user_opt"><%= link_to 'Criar conta', new_usuario_path %></td>
    <td class="user_opt"><%= link_to 'Voltar', controller:"servico", action:"index" %></td>
    <td class="user_opt"><%= link_to 'Inicio', controller:"login", action:"index" %></td>
  </tr>
</table>
