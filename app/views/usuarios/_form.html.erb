<center>
  <%= form_for(@usuario) do |f| %>
    <% if @usuario.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@usuario.errors.count, "error") %> prohibited this usuario from being saved:</h2>

        <ul>
        <% @usuario.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :nome %><br>
      <%= f.text_field :nome %>
    </div>
    <div class="field">
      <%= f.label :matricula %><br>
      <%= f.number_field :matricula %>
    </div>
    <div class="field">
      <%= f.label :diretoria %><br>
      <%= f.select :diretoria, Usuario::DIRETORIAS,
          prompt: 'Selecione' %>
    </div>
    <div class="field">
      <%= f.label :senha %><br>
      <%= f.password_field :senha %>
    </div>
      <div class="field">
      <%= f.label :tipo %><br>
      <%= f.select :tipo, Usuario::USER_TYPES,
          prompt: 'Selecione' %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</center>